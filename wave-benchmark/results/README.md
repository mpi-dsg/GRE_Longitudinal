# Results

Every CSV behind the paper. All measured on identical dual-socket Xeon Gold 6134M servers
(16 physical cores, 755 GB, oneTBB), each otherwise idle while measuring, and every comparison
uses runs from one server. The early directories (`scale400_*` through `lockstats`) come from
one server; `r6`, `r7` and `final_cmp` from a second; `r8` from a third, which also reruns
the baseline and the design so the ablation is compared on one machine.

Generated by `../bench.cpp`. The final run sets (`r6`, `r7`, `r8`) were produced by
`../runs/mq6.sh`, `mq7.sh` and `mq8.sh` on the final binary, which includes the ALEX-OL fixes
in `alexol-sidebuf.patch`; each script's `*_verify.log` in the directory holds the recall and
exhaustive checks.

## Directories

| Directory | Runs | What it is | Used for |
|---|---|---|---|
| `raw/scale400_books` | 13 | 400M SOSD books, bulk 100M, 6 indexes, 1 and 16 threads | headline table and figure |
| `raw/scale400_osm` | 8 | Same on OSM | the boundary result |
| `raw/sweep4m_threads` | 270 | 1M->4M, 6 arms x 5 thread counts x 3 seeds x 3 reps | burstiness vs thread count |
| `raw/remedy_recheck` | 36 | Baseline vs randomized density, counts not percentiles | withdrawal of the C5 claim |
| `raw/remedy_variants` | 144 | delta 0.17 / 0.08 / one-sided | excluding the density-ceiling explanation |
| `raw/pinned_control` | 90 | Same sweep, threads pinned to cores | excluding NUMA placement |
| `raw/btree_remedy` | 144 | Bulk-loaded B+tree, uniform vs randomized leaf fill | the B+tree control |
| `raw/nodesize` | 90 | max_data_node_size 4KB-2MB, counts | node size at 4M |
| `raw/lookup_cost` | 72 | Node size at 50/50 read/insert, reads timed separately | lookup cost of small nodes at 4M |
| `raw/lockstats` | 36 | Insert-restart counters | the concurrency mechanism |
| `raw/ns400` | 16 | Node size (32768 vs 2048 entries) at 400M, books x3 seeds, osm | node size fails at scale |
| `raw/seeds400` | 8 | 400M books baseline and randomized density, seeds 5 and 72 | replication of the headline |
| `raw/mix400` | 4 | Node size at 400M, 50/50 mix | lookup cost at scale |
| `raw/thr` | 54 | 4M at 24 and 32 threads | thread counts past 16 |
<!-- final-rows -->
| `raw/r6/b400` | 30 | Design arms at 400M books (base, side, bgside, bgside2, bgside4, ns2048), 3 seeds, 1 and 16 threads | design tables, Figure 2 |
| `raw/r6/b400ls` | 6 | Restart and rebuild counters (`LOCK2`/`LOCK3`) at 400M | where the restarts go |
| `raw/r6/m4` | 315 | Design arms x 5 thread counts x 3 seeds x 3 reps, 4M | 4M design table |
| `raw/r6/m4mix` | 90 | Same at 50/50, 1 and 16 threads | lookup cost at 4M |
| `raw/r6/m4ls` | 30 | Restart attribution counters, 4M | restart attribution |
| `raw/r6/m4soft` | 18 | Soft-threshold sweep | sensitivity |
| `raw/r6/o400` | 18 | Design arms at 400M osm, 3 seeds | the design on a spread-out distribution |
| `raw/r6/x400` | 7 | Design arms at 400M books, 50/50 mix | lookup cost at scale |
| `raw/r6/eq` | 3 | 12 foreground + 4 background threads, 400M books | equal core budget |
| `raw/r6/sat` | 6 | 32 foreground threads, 400M books | no spare cores |
| `raw/r6/ocmp` | 4 (+1 failed) | XIndex and FINEdex on 400M osm | comparison on osm |
| `raw/final_cmp` | 11 | XIndex and FINEdex at 1 thread, seed 1866 (earlier driver; their code is unchanged) | comparison table |
<!-- /final-rows -->

The `ns400`, `seeds400`, `mix400` and `thr` directories were measured on the second server;
`q14_verify.log` holds their recall checks. Earlier design runs (`mech_*`) predate the ALEX-OL
fixes and are replaced by `r6`.
Current-driver CSVs have 23 fields per `x` row (the 17 below plus `r_*` read columns and
`batch_ns`, `mem`), and `LOCK2` rows carry: retry, retry_smo, retry_ins, parentspin, fg_smo,
bg_smo, bg_skip, smo_ns, smo_max_ns, side_ins, side_read, side_full, smo_split, retry_noside,
split_ns. `analysis/mk_mech.py` and `analysis/mk_panel.py` parse them. `CPU` rows carry tag, seed, threads, batch and CPU nanoseconds over the timed interval.

## Column layout

Most files have 17 columns:

```
tag,index,seed,threads,batch,keys,p50_ns,p99_ns,p999_ns,p9999_ns,max_ns,
n_gt10us,n_gt100us,n_gt1ms,n_ops,batch_ns,mem_bytes
```

`lookup_cost` has 23, inserting six read-path columns before `batch_ns`:

```
r_p50_ns,r_p99_ns,r_p999_ns,r_max_ns,r_gt100us,r_ops
```

`lockstats` files additionally carry `LOCK,` lines:

```
LOCK,tag,seed,threads,batch,keys,retry,parentspin,calls,lock_busy,validate_fail,read_denied
```

Headers are stripped from most files because the runner filters with `grep -E "^x,"`. The
column names above are authoritative.

## Read the counts, not the percentiles

`n_gt10us`, `n_gt100us` and `n_gt1ms` are the primary tail metric. A wave produces roughly one
slow insert per expanding node, and whether a percentile sees them depends on how that count
compares with its rank cutoff, which depends on batch size. On one 400M run, p99.9 over
12.5M-operation batches reports a wave-to-quiet concentration of 1.8x where counting inserts
over 100us reports 926x. Percentiles also flatter any change that merely spreads the same slow
operations more thinly, which produced a result we withdrew. Section 4.4 of the paper has the
full argument.

`mem_bytes` is a free diagnostic on its own: a synchronized cohort expanding together makes a
staircase, an unsynchronized one makes a ramp.

## Superseded, deliberately not kept

Two bodies of runs were discarded rather than stored, because keeping wrong numbers next to
right ones invites mistakes:

- A full sweep using `std::thread` instead of OpenMP. LIPP-OL and SALI index per-thread node
  pools by `omp_get_thread_num()`, which returns 0 for every raw thread, so they shared one
  unsynchronized pool. LIPP-OL crashed; SALI raced silently and returned plausible flat numbers.
- A 400M run measured only with percentiles, before the slow-operation counts existed.

Both are described in `../implementation-notes.md`.

## Regenerating the paper from here

```sh
cd analysis/waves
python3 mktables.py  ../../figures/waves      # 4M sweep tables
python3 mk400b.py    ../../figures/waves      # 400M table
python3 mkfig.py     ../../figures/waves      # 400M figure
cd ../.. && ./check_paper.sh                  # build + structural checks
python3 analysis/waves/verify_numbers.py results/raw   # numbers vs data
```
